# Per-host config for LocalHostName: zis-MacBook-Pro (macOS)
# Sourced from zshenv on this machine only. Runs for ALL shells (env layer).
#
# NO SECRETS. Anything here is committed to git. Secrets go through 1Password
# and the generated ~/.zsh_secrets cache.

# OrbStack installs this local shell integration outside Homebrew/mise.
[[ -r "$HOME/.orbstack/shell/init.zsh" ]] && source "$HOME/.orbstack/shell/init.zsh"
