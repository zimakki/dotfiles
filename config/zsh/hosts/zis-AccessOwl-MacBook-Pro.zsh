# Per-host config for LocalHostName: zis-AccessOwl-MacBook-Pro (macOS)
# Sourced from zshenv on this machine only. Runs for ALL shells (env layer).
#
# NO SECRETS — anything here is committed to git. Secrets go through 1Password
# (see _load_api_keys in zshrc / ~/.zsh_secrets).
#
# Machine-specific environment or PATH entries for this computer go below.
# (none yet — the guarded array in zshenv covers presence-based tool dirs.)
