interface:
  display_name: "Install App"
  short_description: "Install and record an app through dotfiles"
  default_prompt: "Use $install-app to install this application or tool through the dotfiles repo with reproducible package and configuration records."
